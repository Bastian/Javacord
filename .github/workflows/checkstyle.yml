name: Check Codestyle
on: pull_request

jobs:
  checkstyle_job:
    runs-on: ubuntu-latest
    name: Checkstyle job
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Java
        uses: actions/setup-java@v1
        with:
          java-version: 8
      - name: Run check style
        run: ./gradlew checkstyleMain
        continue-on-error: true
      - name: Setup Reviewdog
        uses: reviewdog/action-setup@v1
      - name: Post Review
        run: cat build/javacord-core/reports/checkstyle/main.xml | reviewdog -f=checkstyle -name="reviewdog" -reporter=github-pr-review -filter-mode="file"
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}

